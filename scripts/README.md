# Scripts

This folder is reserved for small helper scripts.

Recommended future scripts:

- `extract-design.ps1`: run `brandmd` against one or more URLs.
- `capture-reference.ps1`: capture desktop/mobile screenshots with Playwright.
- `privacy-check.ps1`: scan generated outputs for forbidden personal placeholders.

The public template does not vendor `brandmd` or `screenshot-to-code`.

